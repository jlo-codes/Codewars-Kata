def cube_checker(volume, side):
    side_checker = False
    if volume <= False:
        side_checker
    else:
        side_cubed = side*side*side
        if side_cubed == volume:
            side_checker = True
        else:
            side_checker
    return side_checker
cube_checker(343,7)