def uni_total(text):
    # ascii_list = []
    print (sum(map(ord,text)))
    # for character in text:
    #     ascii_list.append(ord(character))
    # total = sum(ascii_list)
    # return total

uni_total('aA')